#pragma once

#include <switch.h>

namespace mc::ui {

    class SideBar {
        public:
            SideBar();
            ~SideBar();

            void draw(void);
            
        private:

    };

}
